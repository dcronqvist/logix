// Press reset and enable the clock
push RESET, 300;
set AUTO_CLK=0b1;

// Wait for the program to have jumped
print "waiting...";
wait RAM[0x9f]!=0x00;

// Then just end the simulation.
print "Here";
end;